{
  "name": "arc-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "bash ../deploy.sh auth staging",
    "deploy:prod": "bash ../deploy.sh auth production",
    "migrations:check:prod": "bash ../deploy.sh auth production --check",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1",
    "wrangler": "^3.78.0"
  }
}
