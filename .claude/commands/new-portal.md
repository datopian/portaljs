---
description: "Renamed → /portaljs-new-portal. This alias will be removed next minor release."
allowed-tools: Read
---

# /new-portal → /portaljs-new-portal

This skill was renamed to **`/portaljs-new-portal`** (uniform `portaljs-` prefix across the
PortalJS skill suite, so the whole suite autocompletes under `/portaljs` and never
collides with other tools' commands in the flat command pool).

**Invoke `/portaljs-new-portal` instead.** This alias forwards to it for one minor release, then
goes away. Run `/portaljs-new-portal` now with the same arguments you gave here.
